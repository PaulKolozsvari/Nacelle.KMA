<?xml version="1.0" encoding="UTF-8"?>
<ContentView 
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" 
    xmlns:views="clr-namespace:Nacelle.KMA.UI.Views"
    x:Class="Nacelle.KMA.UI.Views.CustomNavigationBar"
    x:Name="this">
    <ContentView.Content>
        <Grid HeightRequest="50">
            <views:BackNavButton x:Name="BackButton" IsVisible="{Binding IsBackButtonVisible, Source={x:Reference this}}"/>
            <Label x:Name="TitleLabel" FontSize="20" HorizontalOptions="Center" Text="{Binding Title, Source={x:Reference this}}" />
        </Grid>
    </ContentView.Content>
</ContentView>
